# ── 核心：确定性计算（无 AI，必须可独立运行）─────────────────
pandas>=2.0
numpy>=1.26
pyyaml>=6.0
pyarrow>=14.0           # Parquet 存储

# ── 数据源适配器（A 股）────────────────────────────────────
akshare>=1.12           # 主数据源，免费无 Key
baostock>=0.8.8         # 免费备用源：含真实财务+披露日，API 不被东财封 IP
# tushare>=1.4          # 备用源，需积分，按需安装

# ── 技术指标（禁止自实现易错版本）──────────────────────────
pandas-ta>=0.3.14b      # MA/MACD/RSI/KDJ/BOLL...

# ── 回测（禁止自研，避免未来函数/前视偏差）──────────────────
backtrader>=1.9.78

# ── 后端服务（React 前端 + Django 后端）─────────────────────
django>=5.0
djangorestframework>=3.15

# ── 调度 ──────────────────────────────────────────────────
apscheduler>=3.10

# ── AI 适配器（热插拔；按需安装，缺失时优雅降级）────────────
# anthropic>=0.40
# openai>=1.40

# ── 开发/测试 ─────────────────────────────────────────────
pytest>=8.0
